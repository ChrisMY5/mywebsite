<!-- Man Young Oh / 301161472 / COMP229-005 -->

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">  <!-- All codes of this header was copied from https://getbootstrap.com/docs/5.1/components/navbar/-->
    <div class="container-fluid">
      <a class="navbar-brand" href="#">World of Chris MY OH</a> <!-- Logo in this line with href codes if declared-->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent"> 
        <ul class="navbar-nav ms-auto mb-2 mb-1g-0">   
            
        <% if (title == "Home" ) { %>
            <li class="nav-item active">
        <% } else  {%>
          <li class="nav-item">
        <% } %>
            <a class="nav-link" href="/home"><i class="fas fa-laptop-house"></i>   Home</a>
          </li>

          <% if (title == "About" ) { %>
            <li class="nav-item active">
        <% } else  {%>
          <li class="nav-item">
        <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/about"><i class="fas fa-info-circle"></i>   About Me</a>
          </li>

          <% if (title == "Projects" ) { %>
            <li class="nav-item active">
        <% } else  {%>
          <li class="nav-item">
        <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/projects"><i class="fas fa-project-diagram"></i>   Projects</a>
          </li>

          <% if (title == "Services" ) { %>
            <li class="nav-item active">
        <% } else  {%>
          <li class="nav-item">
        <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/services"><i class="fas fa-cogs"></i>   Services</a>
          </li> 

          <% if (title == "Contact" ) { %>
            <li class="nav-item active">
        <% } else  {%>
          <li class="nav-item">
        <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/contact"><i class="fas fa-mobile-alt"></i>   Contact</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>